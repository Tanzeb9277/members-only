<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
        <h1>Become a Member</h1>
        <form action="" method="POST">
            <div class="form-input">
                <input name="username" type="email" value="<%= currentUser.username %>" readonly/>
            </div>

            <div class="form-input">
                <input name="id" type="text" value="<%= currentUser._id %>" readonly/>
            </div>

            
            <div class="form-input">
                <label for="password">Member Passphrase</label>
                <input name="passphrase" type="password" required/>
            </div>
            
            <button>Check</button>
          </form>

</body>
</html>